import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm";import { SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY } from "./config.js";const sb=createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY),$=s=>document.querySelector(s),money=v=>new Intl.NumberFormat("es-MX",{style:"currency",currency:"MXN"}).format(v);const token=new URLSearchParams(location.search).get("token")||sessionStorage.getItem("fiesta15_token");if(!token){$("#statusCard").innerHTML="<h2>Falta el identificador.</h2>"}else load();async function load(){const {data,error}=await sb.rpc("consultar_reservacion_publica",{p_token:token});if(error||!data){$("#statusCard").innerHTML="<h2>No se encontró la reservación.</h2>";return}const r=Array.isArray(data)?data[0]:data;const labels={PENDIENTE_PAGO:"PENDIENTE DE PAGO",COMPROBANTE_ENVIADO:"COMPROBANTE ENVIADO / EN VALIDACIÓN",PAGADO:"PAGO CONFIRMADO",RECHAZADO:"COMPROBANTE RECHAZADO"};$("#statusCard").innerHTML=`<div class="flag-big">${r.estado==="PAGADO"?"✅":"🎟️"}</div><h1>${r.folio}</h1><div class="receipt"><div><span>Contacto</span><strong>${r.contacto_nombre}</strong></div><div><span>Boletos</span><strong>${r.cantidad}</strong></div><div><span>Total</span><strong>${money(r.total)}</strong></div><div><span>Estado</span><strong>${labels[r.estado]||r.estado}</strong></div></div><div id="tickets"></div>`;if(r.estado==="PAGADO")renderTickets(r.boletos||[])}function renderTickets(t){const box=$("#tickets");box.innerHTML="<h2>Tus boletos</h2><p>Presenta un QR por cada asistente.</p>";t.forEach((x,i)=>{const d=document.createElement("div");d.className="ticket-card";d.innerHTML=`<h3>${x.nombre}</h3><div class="qr-target" id="qr-${i}"></div><strong>${x.codigo}</strong>`;box.appendChild(d);new QRCode(document.getElementById(`qr-${i}`),{text:x.codigo,width:190,height:190})})}